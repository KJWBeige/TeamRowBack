//Sam does this

var express = require('express'),
    bodyParser = require('body-parser'),
    logger = require('morgan'),
    app = express(),
    mongoose = require('mongoose')


//add router with express


app.listen(3000)

//call the router
